---
name: "save_data_to_disk"
runner_type: "python-script"
description: "Action to save text (CSV) or JSON payload to disk."
entry_point: "save_data_to_disk.py"
enabled: true
parameters:
  data:
    anyOf:
      - type: "string"
      - type: "object"
      - type: "array"
    required: true
  file_path:
    type: "string"
    description: "The file path to save the text or JSON payload. Must exist and be writeable by user st2."
    required: true